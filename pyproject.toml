[project]
name = "restaurant"
version = "1.0.0"
description = "A basic restaurant reservations manager built with Flask."
dependencies = [
    "email-validator",
    "flask",
    "waitress",
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"